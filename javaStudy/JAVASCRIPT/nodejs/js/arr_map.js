// 배열 요수 추출
const idList = [4, 10, 20]
const userIdList = idList.map((v) => v*2)
// const userIdList = idList.map((v) => `userId+${v}`)
//터미널창 PS C:\01javaStudy\JAVASCRIPT\nodojs\js> node arr_map.js 
console.log(userIdList)

